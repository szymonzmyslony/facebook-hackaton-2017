/**
 * @providesModule CreateEvent
 * @flow
 */
import React from "react";
import { Button, Text, View, ListView } from "react-native";

class CreateEvent extends React.Component {
  static navigationOptions = {
    title: "Create event",
    header: <Button title={"sadssd"} onPress={() => {}} />
  };
  render() {
    return (
      <View>
        <Text>asdas</Text>
      </View>
    );
  }
}
